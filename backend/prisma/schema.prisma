// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  // output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(uuid())  @map("user_id")
  userName String @map("username") @unique
  email String @map("user_email") @unique
  password String @map("user_password")
  firstName String @map("first_name")
  lastName String @map("last_name")

  posts Posts[]

  @@map("users")
}

model Posts{
  id String @default(uuid()) @id @map("blogPost_id")
  title String @map("blog_title")
  synopsis String @map("blogPost_synopsis")
  content String @map("blog_info")
  featuredImageURL String @map("blog_image_url")
  isDeleted Boolean @default(false) @map("is_deleted")
  lastUpdated DateTime @updatedAt @map("last_updated_on")
  creationDate DateTime @default(now()) @map("created_at")

  userId String 
  
  blogAuthor User @relation(fields: [userId], references: [id])

  @@map("posts")
}